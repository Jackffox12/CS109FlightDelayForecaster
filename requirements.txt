# Minimal dependencies for Vercel serverless deployment
httpx==0.27.0
python-dotenv==1.0.0

# Heavy ML dependencies removed for serverless compatibility
# For full ML functionality, consider deploying backend separately 